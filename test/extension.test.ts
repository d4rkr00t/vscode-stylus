import {expect} from 'chai';
// import * as vscode from 'vscode';
// import * as myExtension from '../src/extension';

describe('my first vscode test', () => {
  it('should display Hello World to the user', () => {
    expect(true).to.equal(true, 'true === true');
  });
});
