import {expect} from 'chai';
// import {ExtensionContext} from 'vscode';
// import * as extension from '../src/main';

describe('extension load', () => {
  it('should load the extension on activate', () => {
    // extension.activate()
    expect(true).to.equal(true, 'true === true');
  });
});
